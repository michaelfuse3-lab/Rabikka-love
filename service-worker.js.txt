self.addEventListener('install', function(e) {
  e.waitUntil(
    caches.open('love-app').then(function(cache) {
      return cache.addAll([
        '/',
        '/index.html'
      ]);
    })
  );
});